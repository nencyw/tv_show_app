<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="<%= root_path%>">Your Reminder</a>
    </div>
    <ul class="nav navbar-nav navbar-right">
      <% if current_user.present?%>
        <li><%= link_to destroy_user_session_path, method: :delete do %></span> Sign out
        <%end %></li>
      <% else %>
        <li><%= link_to user_session_path do %><span class="glyphicon glyphicon-log-in"></span> Login
        <%end %></li>
      <%end%>
    </ul>
  </div>
</nav>